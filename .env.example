# Environment Configuration for Heroku Deploy
# Copy this file to .env for local development

# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/database_name

# Application Environment
NODE_ENV=production

# SSL Configuration (for Heroku PostgreSQL)
DATABASE_SSL=require
DATABASE_SSL_REJECT_UNAUTHORIZED=false

# Build Configuration removida - drizzle-kit agora está nas dependencies principais

# Session Secret (REQUIRED for production)
SESSION_SECRET=your-super-secret-session-key-here

# Application Port (Heroku sets this automatically)
PORT=5000